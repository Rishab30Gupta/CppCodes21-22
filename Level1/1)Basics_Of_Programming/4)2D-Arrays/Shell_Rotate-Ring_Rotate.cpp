#include<bits/stdc++.h>
using namespace std;
signed main() {
#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif

	// int n, m; cin >> n >> m;
	// int arr[n][m];
	// for (int i = 0; i < n; i++) {
	// 	for (int j = 0; j < m; j++) {
	// 		cin >> arr[i][j];
	// 	}
	// }
	// int shell_number, d; cin >> shell_number >> d;
	// //step 1-> make 1d array filling the elmeents(from spiral display)


	// //step 2->rotate the 1d array
	// //step 3->again fill the shell using the rotated array
	string str = "helloworld";
	cout << str.substr(5, 3);



}